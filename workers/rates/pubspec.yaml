name: rates
description: Worker that fetches public exchange-rate sources and publishes an append-only NDJSON feed (ADR-0020).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  shared_kernel:
    path: ../../packages/shared_kernel
  tasas:
    path: ../../packages/tasas
  decimal: ^3.2.4
  http: ^1.2.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
