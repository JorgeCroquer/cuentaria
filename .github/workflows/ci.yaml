name: CI

on:
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          # Pinned to the Flutter in the Conuco sandbox image: CI and the AFK
          # build gate must run the same toolchain, or analyze diverges (a member
          # deprecated upstream fails CI while the sandbox still accepts it).
          # Bump deliberately, together with the image.
          flutter-version: '3.29.1'
          cache: true

      - name: Install Melos
        run: dart pub global activate melos

      - name: Bootstrap workspace
        run: melos bootstrap

      - name: Check formatting
        run: dart format --output=none --set-exit-if-changed .

      - name: Static analysis
        run: melos run analyze

      - name: Run tests
        run: melos run test

      - name: Build Web
        working-directory: ./apps/cuentaria_app
        run: flutter build web
