name: backup
description: Backup File format for Cuentaria — pure NDJSON reader/writer (ADR-0021).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  shared_kernel:
    path: ../shared_kernel

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
