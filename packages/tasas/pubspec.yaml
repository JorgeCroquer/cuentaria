name: tasas
description: Exchange rate observations context for Cuentaria — the embryo of S1 (ADR-0016).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  shared_kernel:
    path: ../shared_kernel
  decimal: ^3.2.4
  equatable: ^2.1.0
  drift: ^2.31.0
  sqlite3: ^2.9.4
  http: ^1.2.0
dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
  drift_dev: ^2.31.0
  build_runner: ^2.4.0
