name: contabilidad
description: The cash accounting bounded context for Cuentaria.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  shared_kernel:
    path: ../shared_kernel
  event_bus:
    path: ../event_bus
  decimal: ^3.2.4
  uuid: ^4.5.1

  equatable: ^2.1.0
  drift: ^2.31.0
  sqlite3: ^2.9.4
dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
  drift_dev: ^2.31.0
  build_runner: ^2.4.0
