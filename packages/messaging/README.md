# messaging

Provides `EventBus` and `DomainEvent` for the Cuentaria composition root.

This package defines the interfaces for the in-process event bus used for cross-context communication in the application, keeping domains decoupled.
