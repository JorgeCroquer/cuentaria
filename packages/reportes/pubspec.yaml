name: reportes
description: Read-only reporting context for Cuentaria — pure reporting engine, mes calendario en costo real (ADR-0024).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  shared_kernel:
    path: ../shared_kernel
  equatable: ^2.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
