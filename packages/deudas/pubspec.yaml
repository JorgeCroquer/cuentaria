name: deudas
description: Read-only debt projection context for Cuentaria — pure valuation engine (ADR-0022).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  shared_kernel:
    path: ../shared_kernel
  decimal: ^3.2.4
  equatable: ^2.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
