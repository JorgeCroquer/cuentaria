name: shared_kernel
description: A pure Dart package containing the shared domain value objects for Cuentaria.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  equatable: ^2.0.5

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
